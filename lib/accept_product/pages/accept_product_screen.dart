import 'package:flutter/material.dart';

class AcceptProductScreen extends StatelessWidget {
  const AcceptProductScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('AcceptProductScreen'),
      ),
    );
  }
}
